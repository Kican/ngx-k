import { InputDateTimeDescriptor } from './input-date-time-descriptor';

describe('InputDateTimeDescriptor', () => {
  it('should create an instance', () => {
    expect(new InputDateTimeDescriptor()).toBeTruthy();
  });
});
