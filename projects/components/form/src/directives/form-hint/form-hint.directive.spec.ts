import { FormHintDirective } from './form-hint.directive';

describe('HintDirective', () => {
  it('should create an instance', () => {
    const directive = new FormHintDirective();
    expect(directive).toBeTruthy();
  });
});
