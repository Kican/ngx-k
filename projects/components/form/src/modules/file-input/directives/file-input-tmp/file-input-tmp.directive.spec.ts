import { FileInputTmpDirective } from './file-input-tmp.directive';

describe('FileInputTmpDirective', () => {
  it('should create an instance', () => {
    const directive = new FileInputTmpDirective();
    expect(directive).toBeTruthy();
  });
});
