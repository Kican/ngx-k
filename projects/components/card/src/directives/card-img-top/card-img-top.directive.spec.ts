import { CardImgTopDirective } from './card-img-top.directive';

describe('CardImgTopDirective', () => {
  it('should create an instance', () => {
    const directive = new CardImgTopDirective();
    expect(directive).toBeTruthy();
  });
});
