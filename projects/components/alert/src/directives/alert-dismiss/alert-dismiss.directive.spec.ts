import { AlertDismissDirective } from './alert-dismiss.directive';

describe('AlertDismissDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertDismissDirective();
    expect(directive).toBeTruthy();
  });
});
