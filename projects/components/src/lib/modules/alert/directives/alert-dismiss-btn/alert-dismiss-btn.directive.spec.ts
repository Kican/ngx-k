import { AlertDismissBtnDirective } from './alert-dismiss-btn.directive';

describe('AlertDismissBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertDismissBtnDirective();
    expect(directive).toBeTruthy();
  });
});
