import { KSelectDirective } from './kselect.directive';

describe('KSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new KSelectDirective();
    expect(directive).toBeTruthy();
  });
});
