import { HttpSelectDataProvider } from './http-select-data-provider';

describe('HttpSelectDataProvider', () => {
  it('should create an instance', () => {
    expect(new HttpSelectDataProvider()).toBeTruthy();
  });
});
