import {ValueSelectDataProvider} from './value-select-data-provider';

describe('ValueSelectDataProvider', () => {
	it('should create an instance', () => {
		expect(new ValueSelectDataProvider()).toBeTruthy();
	});
});
