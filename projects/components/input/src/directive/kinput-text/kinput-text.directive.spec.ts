import { KInputTextDirective } from './kinput-text.directive';

describe('KInputTextDirective', () => {
  it('should create an instance', () => {
    const directive = new KInputTextDirective();
    expect(directive).toBeTruthy();
  });
});
