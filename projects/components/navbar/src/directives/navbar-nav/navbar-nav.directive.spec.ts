import { NavbarNavDirective } from './navbar-nav.directive';

describe('NavbarNavDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarNavDirective();
    expect(directive).toBeTruthy();
  });
});
