import { NavbarNavItemDirective } from './navbar-nav-item.directive';

describe('NavbarNavItemDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarNavItemDirective();
    expect(directive).toBeTruthy();
  });
});
