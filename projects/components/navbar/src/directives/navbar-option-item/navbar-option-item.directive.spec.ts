import { NavbarOptionItemDirective } from './navbar-option-item.directive';

describe('NavbarOptionItemDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarOptionItemDirective();
    expect(directive).toBeTruthy();
  });
});
