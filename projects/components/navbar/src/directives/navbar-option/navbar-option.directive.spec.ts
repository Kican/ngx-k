import { NavbarOptionDirective } from './navbar-option.directive';

describe('NavbarOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarOptionDirective();
    expect(directive).toBeTruthy();
  });
});
