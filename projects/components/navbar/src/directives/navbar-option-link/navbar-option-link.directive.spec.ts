import { NavbarOptionLinkDirective } from './navbar-option-link.directive';

describe('NavbarOptionLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarOptionLinkDirective();
    expect(directive).toBeTruthy();
  });
});
