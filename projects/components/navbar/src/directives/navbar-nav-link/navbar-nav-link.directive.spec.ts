import { NavbarNavLinkDirective } from './navbar-nav-link.directive';

describe('NavbarNavLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarNavLinkDirective();
    expect(directive).toBeTruthy();
  });
});
