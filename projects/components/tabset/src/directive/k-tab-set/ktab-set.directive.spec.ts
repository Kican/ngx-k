import { KTabSetDirective } from './ktab-set.directive';

describe('KTabSetDirective', () => {
  it('should create an instance', () => {
    const directive = new KTabSetDirective();
    expect(directive).toBeTruthy();
  });
});
