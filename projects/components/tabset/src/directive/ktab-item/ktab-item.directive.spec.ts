import { KTabItemDirective } from './ktab-item.directive';

describe('KTabItemDirective', () => {
  it('should create an instance', () => {
    const directive = new KTabItemDirective();
    expect(directive).toBeTruthy();
  });
});
