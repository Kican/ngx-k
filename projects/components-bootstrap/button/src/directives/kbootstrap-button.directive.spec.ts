import { KBootstrapButtonDirective } from './kbootstrap-button.directive';

describe('KBootstrapButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new KBootstrapButtonDirective();
    expect(directive).toBeTruthy();
  });
});
