import { KBootstrapTabItemDirective } from './kbootstrap-tab-item.directive';

describe('KBootstrapTabItemDirective', () => {
  it('should create an instance', () => {
    const directive = new KBootstrapTabItemDirective();
    expect(directive).toBeTruthy();
  });
});
