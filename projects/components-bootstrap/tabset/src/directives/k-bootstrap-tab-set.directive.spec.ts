import {KBootstrapTabSetDirective} from './k-bootstrap-tab-set.directive';

describe('KTabSetDirective', () => {
	it('should create an instance', () => {
		const directive = new KBootstrapTabSetDirective();
		expect(directive).toBeTruthy();
	});
});
